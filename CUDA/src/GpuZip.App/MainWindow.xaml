<Window x:Class="GpuZip.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="GPUZIP File Manager"
        Width="1180"
        Height="760"
        MinWidth="900"
        MinHeight="600"
        WindowStartupLocation="CenterScreen"
        Background="#F0F0F0"
        FontFamily="Segoe UI"
        FontSize="12">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="150" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" Background="#F0F0F0" BorderBrush="#D0D0D0" BorderThickness="0,0,0,1">
            <MenuItem Header="_File">
                <MenuItem Header="Open archive..." Click="OpenArchive_Click" />
                <MenuItem Header="Open original 7-Zip File Manager" Click="OpenSevenZipGui_Click" />
                <Separator />
                <MenuItem Header="Create archive..." Click="CreateArchive_Click" />
            </MenuItem>
            <MenuItem Header="_Edit">
                <MenuItem Header="Add files..." Click="AddFiles_Click" />
                <MenuItem Header="Add folder..." Click="AddFolder_Click" />
                <MenuItem Header="Remove selected input" Click="RemoveSelected_Click" />
            </MenuItem>
            <MenuItem Header="_View">
                <MenuItem Header="Classic 7-Zip layout" IsChecked="True" IsEnabled="False" />
            </MenuItem>
            <MenuItem Header="_Tools">
                <MenuItem Header="Test archive" Click="Test_Click" />
            </MenuItem>
            <MenuItem Header="_Help">
                <MenuItem Header="GPUZIP · adaptive CPU/CUDA compression" IsEnabled="False" />
            </MenuItem>
        </Menu>

        <Border Grid.Row="1" Background="#F0F0F0" BorderBrush="#C8C8C8" BorderThickness="0,0,0,1" Padding="6,4">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Right" Orientation="Vertical" Margin="12,0,4,0" VerticalAlignment="Center">
                    <CheckBox x:Name="CudaCheck" Content="CUDA" IsChecked="True" FontWeight="SemiBold" />
                    <CheckBox x:Name="MaximumPerformanceCheck" Content="Maximum performance" IsChecked="True" Margin="0,3,0,0" />
                </StackPanel>

                <StackPanel DockPanel.Dock="Right" Margin="0,0,8,0" VerticalAlignment="Center">
                    <TextBlock Text="Archive format" Margin="0,0,0,2" />
                    <ComboBox x:Name="FormatComboBox" Width="165" SelectedIndex="0">
                        <ComboBoxItem Content="GPUZIP (.gpuz)" Tag="gpuz" />
                        <ComboBoxItem Content="7-Zip (.7z)" Tag="7z" />
                        <ComboBoxItem Content="ZIP (.zip)" Tag="zip" />
                        <ComboBoxItem Content="TAR (.tar)" Tag="tar" />
                        <ComboBoxItem Content="GZip (.gz)" Tag="gzip" />
                        <ComboBoxItem Content="BZip2 (.bz2)" Tag="bzip2" />
                        <ComboBoxItem Content="XZ (.xz)" Tag="xz" />
                    </ComboBox>
                </StackPanel>

                <WrapPanel VerticalAlignment="Center">
                    <Button Width="70" Height="52" Margin="2" Click="AddFiles_Click">
                        <StackPanel>
                            <TextBlock Text="+" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" />
                            <TextBlock Text="Add" HorizontalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Width="70" Height="52" Margin="2" Click="Extract_Click">
                        <StackPanel>
                            <TextBlock Text="↓" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" />
                            <TextBlock Text="Extract" HorizontalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Width="70" Height="52" Margin="2" Click="Test_Click">
                        <StackPanel>
                            <TextBlock Text="✓" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" />
                            <TextBlock Text="Test" HorizontalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Width="70" Height="52" Margin="2" Click="OpenArchive_Click">
                        <StackPanel>
                            <TextBlock Text="⌂" FontSize="20" HorizontalAlignment="Center" />
                            <TextBlock Text="Open" HorizontalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Width="70" Height="52" Margin="2" Click="AddFolder_Click">
                        <StackPanel>
                            <TextBlock Text="▣" FontSize="20" HorizontalAlignment="Center" />
                            <TextBlock Text="Folder" HorizontalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Width="86" Height="52" Margin="2" Click="CreateArchive_Click">
                        <StackPanel>
                            <TextBlock Text="⚡" FontSize="18" HorizontalAlignment="Center" />
                            <TextBlock Text="Compress" HorizontalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <Button Width="90" Height="52" Margin="2" Click="OpenSevenZipGui_Click" ToolTip="Open the original 7-Zip File Manager bundled with GPUZIP">
                        <StackPanel>
                            <TextBlock Text="7z" FontSize="17" FontWeight="Bold" HorizontalAlignment="Center" />
                            <TextBlock Text="7-Zip GUI" HorizontalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </WrapPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="2" Background="White" BorderBrush="#BDBDBD" BorderThickness="0,0,0,1" Padding="7,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="Path:" VerticalAlignment="Center" Margin="0,0,7,0" />
                <Border Grid.Column="1" BorderBrush="#A0A0A0" BorderThickness="1" Background="White" Padding="6,3">
                    <TextBlock x:Name="ArchiveTitle" Text="Computer / No archive open" TextTrimming="CharacterEllipsis" />
                </Border>
                <TextBlock x:Name="ArchiveSummary" Grid.Column="2" Margin="12,0,2,0" VerticalAlignment="Center" Foreground="#555555" />
            </Grid>
        </Border>

        <Border Grid.Row="3" Background="White" BorderBrush="#A8A8A8" BorderThickness="0,0,0,1">
            <DataGrid x:Name="ArchiveList"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      SelectionMode="Extended"
                      SelectionUnit="FullRow"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      RowHeaderWidth="0"
                      BorderThickness="0"
                      Background="White"
                      AlternatingRowBackground="#FAFAFA">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="3*" />
                    <DataGridTextColumn Header="Size" Binding="{Binding Size}" Width="105" />
                    <DataGridTextColumn Header="Packed Size" Binding="{Binding Packed}" Width="105" />
                    <DataGridTextColumn Header="Type" Binding="{Binding Type}" Width="95" />
                    <DataGridTextColumn Header="Modified" Binding="{Binding Modified}" Width="150" />
                    <DataGridTextColumn Header="Method" Binding="{Binding Method}" Width="2*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <GroupBox Grid.Row="4" Header="Files queued for Add / Compress" Margin="5,4,5,4" Background="#F0F0F0">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ListBox x:Name="InputList" SelectionMode="Extended" BorderBrush="#A8A8A8" />
                <StackPanel Grid.Column="1" Margin="6,0,0,0">
                    <Button Content="Add files..." Width="105" Margin="0,0,0,4" Click="AddFiles_Click" />
                    <Button Content="Add folder..." Width="105" Margin="0,0,0,4" Click="AddFolder_Click" />
                    <Button Content="Remove" Width="105" Click="RemoveSelected_Click" />
                </StackPanel>
            </Grid>
        </GroupBox>

        <Border Grid.Row="5" Background="#F0F0F0" BorderBrush="#BDBDBD" BorderThickness="0,1,0,0" Padding="6,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="280" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock x:Name="StatusText" Text="Ready" />
                    <TextBlock x:Name="DetailText" Text="7-Zip engine and GPUZIP codec ready" FontSize="11" Foreground="#555555" TextTrimming="CharacterEllipsis" />
                </StackPanel>
                <Border Grid.Column="1" Background="#E8E8E8" BorderBrush="#BDBDBD" BorderThickness="1" Padding="8,3" Margin="8,0">
                    <TextBlock x:Name="CudaStatusText" Text="CUDA enabled · CPU fallback" VerticalAlignment="Center" />
                </Border>
                <ProgressBar x:Name="OperationProgress" Grid.Column="2" Minimum="0" Maximum="100" Height="16" VerticalAlignment="Center" />
            </Grid>
        </Border>
    </Grid>
</Window>
